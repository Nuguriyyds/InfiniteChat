# 角色设定
你现在是一个资深的 Java 后端架构师和测试开发专家。请协助我完成微服务架构下的性能压测准备工作，包括制定压测步骤和编写海量测试数据生成脚本。

# 1. 系统架构与技术栈上下文
- **开发语言**: Java 1.8 / Spring Boot 2.x / Spring Cloud Alibaba
- **数据库**: MySQL 8.x (InnoDB)
- **缓存**: Redis
- **核心业务场景**: IM 系统的单聊消息发送 / 用户高并发登录鉴权 / 群聊消息拉取 / 用户抢红包
- **机器配置**: 一台2核4G的服务器搭了基建，使用docker安装了所有的中间件，一台2核8G的服务器用来跑业务代码。IP地址分别为8G公网：47.98.243.139，8G私网：172.31.130.141。4G公网47.97.100.24，4G私网：172.30.233.168
- **当前瓶颈/担忧**: 担心 MySQL 插入单点瓶颈、Redis 内存被打满、或者微服务网关的吞吐量不足、CPU瓶颈等

# 2. 数据库表结构 (非常重要)
为了让你帮我写出准确的批量插入脚本，以下是相关核心表的 DDL（请基于此生成数据）：InfiniteChat.sql

# 3. 批量造数据需求 (Data Generation)
请帮我编写一个能在测试环境快速生成海量数据的脚本或程序。

目标数据量: [例如：生成 1000个真实模拟用户，以及 1000 条历史聊天记录]

数据特征要求:

user_id 必须全局唯一。

时间戳需要随机分布在过去 30 天内。

[其他业务约束，例如：密码需要经过 BCrypt 加密，或者 Redis 里需要同步预热这些用户的状态标识]

期望的生成方式: 请推荐最高效的生成方式（例如：Java 原生多线程 JDBC 批处理、存储过程、还是 Python Faker 脚本？），并给出完整可执行的代码。

# 4. 压测实施步骤需求 (Load Testing Steps)
请为我梳理一套严谨的压测方案。

压测目标: 验证该接口能否在 [例如：5000 QPS] 下保持 99% 的请求响应时间在 [例如：50ms] 以内。

推荐工具: [如果不确定，可以让 AI 推荐，或者指定：请基于 JMeter 或 wrk 给出具体步骤]

输出要求:

压测工具的具体配置步骤（如线程数、Ramp-up 时间设置）。

需要监控的系统核心指标有哪些（如 CPU、Load、JVM GC、MySQL IOPS）。

压测过程中可能出现的性能拐点预测及排查思路。

# 5. 务必使用中文回复